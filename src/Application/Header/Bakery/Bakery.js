import React from 'react';
import FFmpegSettings from './FFmpegSettings';

const Bakery = () => {

	return (
		<section id="bakery">
			<FFmpegSettings />
		</section>
	       );
};

export default Bakery;

